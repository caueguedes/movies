# # Place all the behaviors and hooks related to the matching controller here.
# # All this logic will automatically be available in application.js.
# # You can use CoffeeScript in this file: http://coffeescript.org/


            # $('#'+event.target.parentNode.id).html("<%= j render partial: 'like', locals => {:id => id} %>")
    
# # $(() => $("a[data-remote]").on("ajax:success", function(event) {
# #   if (event.target.className === "fa fa-heart text-danger") {
# #       //like
# #       alert('funfou destroylike');
# #       return $('#'+event.target.parentNode.id).html('$("<%= j render(:partial => 'unlike', :locals => {:id => "like_destroy_params" }) %>")');
# #   } else {
# #       //unlike
# #       alert('funfou destroyunlike');
# #       return $('#'+event.target.parentNode.id).html('<%= j render(:partial => 'like', :locals => {:id =>  "like_destroy_params" }) %>');
# #     }
# # }));

